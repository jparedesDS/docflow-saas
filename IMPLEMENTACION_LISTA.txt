════════════════════════════════════════════════════════════════════════════════
FIX GAIA TNEF - IMPLEMENTADO
════════════════════════════════════════════════════════════════════════════════

✅ CAMBIOS APLICADOS:

1. docflow/backend/services/imap_service.py
   - Función get_html_body() (líneas 82-105)
   - NUEVO: try/except alrededor de TNEF parsing
   - NUEVO: fallback htmlbody → rtfbody → body
   - NUEVO: conversión RTF a HTML usando striprtf
   - NUEVO: logging para debugging

2. docflow/backend/requirements.txt (CREADO)
   - striprtf==0.0.26 (para convertir RTF → texto/HTML)
   - Resto de dependencias (fastapi, tnefparse, etc.)

════════════════════════════════════════════════════════════════════════════════
PASOS SIGUIENTES (5 min):
════════════════════════════════════════════════════════════════════════════════

[1] INSTALAR DEPENDENCIAS
────────────────────────────────────────────────────────────────────────────────
En terminal, ejecuta:

  cd docflow/backend
  pip install -r requirements.txt

O solo lo nuevo:

  pip install striprtf==0.0.26

[2] REINICIAR BACKEND
────────────────────────────────────────────────────────────────────────────────
Presiona Ctrl+C en la terminal del backend (si está corriendo)

Luego ejecuta:

  python -m uvicorn main:app --reload

Debería ver:
  INFO:     Application startup complete

[3] PROBAR CON UI
────────────────────────────────────────────────────────────────────────────────
- Abre http://localhost:3000
- Ve a Email Assistants > Tab de emails
- Busca un email GAIA (sender que no sea egesdoc@grupotr.es)
- Click en "Preview"
- Verifica que aparezca tabla con datos

[4] DEBUGAR SI HAY PROBLEMAS
────────────────────────────────────────────────────────────────────────────────
Ver logs en terminal del backend para mensajes como:
  - "TNEF: extraído htmlbody"
  - "TNEF: rtfbody convertido a HTML"
  - "TNEF: usando body plano"
  - "Error decodificando TNEF: ..."

════════════════════════════════════════════════════════════════════════════════
QUÉ PASA INTERNAMENTE:
════════════════════════════════════════════════════════════════════════════════

Cuando recibe email GAIA:

1. Intenta extraer text/html directo ← TR, ACONEX, SENDOC (no cambia)
2. Si no → busca TNEF y extrae htmlbody ← NUEVO
3. Si no tiene htmlbody → convierte rtfbody a HTML ← NUEVO
4. Si no → usa body plano formateado como HTML ← NUEVO
5. Si todo falla → devuelve "" vacío (no rompe)

════════════════════════════════════════════════════════════════════════════════
ENDPOINT DEBUG (opcional):
════════════════════════════════════════════════════════════════════════════════

Si quieres inspecionar un email específico:

  GET http://localhost:8000/api/transmittals/emails/{UID}/debug

Devuelve estructura MIME completa para debugging.

════════════════════════════════════════════════════════════════════════════════
